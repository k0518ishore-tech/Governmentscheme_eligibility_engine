// Deliberately contains no sample schemes. Add verified scheme records through
// the authenticated admin interface or import an explicitly reviewed data file.
console.log('No sample scheme records are bundled. Use the admin interface to add verified scheme information.');
