// ============================================================
// SchemeGuide — Admin User Management (stub redirect)
// ============================================================
// Full implementation is in categoryManagement.js
function renderUserManagementPage(container) {
  return renderUserManagement(container);
}
